<% provide(:title, 'Feedback') %>
<h1>Feedback</h1>
<p>
<%= form_for @feedback, :url => { :action => "submit_feedback" } do |f| %>
	<div class="field">
		<%= f.text_area :content %>
	</div>
	<%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>
</p>